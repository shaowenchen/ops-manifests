apiVersion: crd.chenshaowen.com/v1
kind: TaskRun
metadata:
  name: alert-gpu-drop
  namespace: ops-system
spec:
  desc: alert gpu drop every hour at 30 minutes
  crontab: "30 * * * *"
  taskRef: alert-gpu-drop
apiVersion: crd.chenshaowen.com/v1
kind: Task
metadata:
  name: list-pods-limits
  namespace: ops-system
spec:
  desc: list pod resources limits in cluster
  host: anymaster
  variables:
    namespace:
      required: true
  steps:
    - name: list-pods-resources
      when: ${namespace} != "all"
      content: kubectl get pod -o custom-columns='POD-NAME:.metadata.name,NAMESPACE:.metadata.namespace,RESOURCES-LIMITS:.spec.containers[*].resources.limits' -n ${namespace}
    - name: list-all-pods-resources
      when: ${namespace} == "all"
      content: kubectl get pod -o custom-columns='POD-NAME:.metadata.name,NAMESPACE:.metadata.namespace,RESOURCES-LIMITS:.spec.containers[*].resources.limits' -A

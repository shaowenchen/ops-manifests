apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: preload-image
  namespace: ops-system
  labels:
    ops/copilot: enabled
spec:
  desc: Preload images in cluster
  tasks:
    - name: preload
      desc: Preload images in cluster
      taskRef: preload-image
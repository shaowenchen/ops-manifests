apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: delete-pod
  namespace: ops-system
  labels:
    ops/copilot: enabled
spec:
  desc: Restart or delete a pod in the cluster
  tasks:
    - name: get-pod
      desc: Get pod information
      taskRef: get-pod
    - name: delete-pod
      desc: Delete pod
      taskRef: delete-pod

apiVersion: crd.chenshaowen.com/v1
kind: Pipeline
metadata:
  name: clear-mount
  namespace: ops-system
  labels:
    ops/copilot: enabled
spec:
  desc: Clear mount points
  tasks:
    - name: clear-mount-point
      desc: Clear mount points
      taskRef: clear-mount
